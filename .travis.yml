language: node_js
dist: trusty
node_js:
  - "8"
install:
  - npm install
script:
  - hexo clean
  - yuque-hexo sync
  - yuque-hexo clean
  - hexo g
  - chmod +x ./deploy.sh
  - \cp -f assets/CNAME public/CNAME
  - ./deploy.sh > /dev/null
cache:
  directories:
    - node_modules